/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "mahalaxmi-krushi-udyog.teammo.in"],
  },
};

module.exports = nextConfig;
